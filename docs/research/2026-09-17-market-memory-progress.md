# 全市场与内存行情测试进度（未完成验收）

本文件是脱敏阶段记录，不包含账户、密钥、原始行情或运行配置。

## 已执行

- 用户确认：2026-08-13、2026-08-14；沪深北 A 股、ETF、指数；1d/1m/5m及复权事件。
- 真实四板块快照去重7,872证券；冻结55,104采集单元。
- 独立历史入口首批100单元耗时47.891秒：97保存，3项状态文件读取PermissionError。
  已保存83历史文件/8,119行、14复权文件/116事件。83历史单元中69项原任务有下载尝试
  并通过最终校验，14项缓存命中。
- 外部状态读取增加最多250ms、受总预算约束的只读重试；没有换原生下载ID或修改worker。
  三个原请求后来均确认有有效returned记录，尚需由采集任务同ID对账更新。
- 后续历史桥未响应，外部程序已停止继续提交。摘要97完成、21失败、54,986剩余；
  其中后新增18个失败为请求超时，不能解释为证券没有历史数据。策略状态待用户确认。
- QMT内置Python3.6.8实际执行10,000合成样本，逐条一致；往返p50约47ms、p99/max约79ms。
  256KiB边界、队列满载、损坏/截断/协议/旧会话/重复序号/超长拒绝通过。
- 本机管道DACL读回、独立受限令牌进程访问拒绝通过；未冒充另一登录用户的终端测试。
- 无行情API调用的负载回调间隔p50约47ms、max约79ms；空闲间隔p50约47ms、max约63ms。

## 尚未通过

停止观察器曾将本地TimeoutError（OSError子类）误判为peer关闭。该证据已明确撤销，
旧报告保留作审计；修复后的观察器有版本标记，重启检查拒绝旧报告。独立回归通过。
随后使用新版观察器重新执行：实际观察到未完成请求的对端关闭，用户确认
`resources_closed=True`，重启产生新实例；旧会话被拒绝，新连接恢复成功。

M1b总体状态已为 **named_pipe_verified**。随后完成了[统一入口两证券真实短采样](2026-09-17-unified-collector-live.md)，
60 秒 236 条新鲜行情、39 次源时间更新，归档及退订通过；这不是全市场或全天行情验收。
行情APP、生命周期适配器与外部消费者已实现并独立审查；目前模式为
`poll_snapshot`，不保证逐笔增量完整性，不提供交易。

全市场采集仍未完成。统一入口已恢复并按原冻结计划续采；失败熔断、早期失败优先恢复、复权终态
只读请求的追加审计刷新已通过独立测试，原生下载ID保持不变。

用户要求收敛为一个 QMT 脚本。统一入口复用未改动的内存核心和历史 worker，采用一个
定时回调；私有运行包生成于项目所在 D 盘的独立受保护目录，源码仍在项目工作目录。
历史停滞时的只读诊断发现历史 runtime 无锁持有者、无 running 原生请求，尚不能判定
策略调度停止的根因；不将 UI 显示运行等同于实际请求已处理。

## 代码验证

外部Python3.10.20，完整命令：

```powershell
python -m pytest tests/ experiments/qualification_v1/test_qualification.py -q
```

提交前完整回归 **513 passed in 91.85s**；`git diff --check`通过。合成、外部Windows真实管道、
QMT实际合成、历史缓存命中和实际下载分别记录，不将离线回归计为行情终端验收。
